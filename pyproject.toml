[project]
name = "faster-mokuro"
authors = [
    {name = "Jan Lancelot", email = "janlancelot@users.noreply.github.com"},
    {name = "Maciej Budyś", email = "kha-white@mail.com"},
]
description = "Fast manga reader OCR and bundling with batched inference & Apple Silicon MPS acceleration"
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
classifiers = [
    "Programming Language :: Python :: 3",
]

dynamic = ["version"]
dependencies = [
    "fire",
    "loguru",
    "manga-ocr>=0.1.7",
    "natsort",
    "numpy",
    "opencv-python>=4.1.2",
    "packaging",
    "Pillow>=11.3",
    "pyclipper",
    "requests",
    "scipy",
    "shapely",
    "torch>=1.7.0",
    "torchsummary",
    "torchvision>=0.8.1",
    "transformers>=4.25.0",
    "tqdm>=4.41.0",
    "yattag",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[project.urls]
Homepage = "https://github.com/JanLancelot/faster-mokuro"
Repository = "https://github.com/JanLancelot/faster-mokuro"

[build-system]
requires = ["setuptools >= 61.0.0", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["mokuro*", "comic_text_detector*"]

[tool.setuptools.package-data]
comic_text_detector = ["LICENSE"]

[tool.setuptools.dynamic]
version = {attr = "mokuro._version.__version__"}

[tool.ruff]
line-length = 120
indent-width = 4
extend-exclude = ["comic_text_detector", "notebooks"]

[project.scripts]
mokuro = "mokuro.__main__:main"
faster-mokuro = "mokuro.__main__:main"